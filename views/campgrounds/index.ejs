<% include ../partials/header %>

<div class='container'>
  <header class='jumbotron'>
      <h1>Welcome To YelpCamp</h1>
    <p>View our hand-picked campgrounds from all over the UK!</p>
    <p>
      <a class='btn btn-success btn-lg' href='/campgrounds/new'>Add New Campground</a>
    </p>
  </header>

  <div class='row'>
    <% campgrounds.forEach(campground => { %>
      <div class='col-md-4 col-sm-6'>
        <div class='img-thumbnail'>
          <img class='camp_image' src='<%= campground.image %>'>
          <div class='caption'>
            <h4 class='d-flex justify-content-center'><%= campground.name %></h4>
          </div>
          <p class="px-5">
            <a href='/campgrounds/<%= campground._id%>' class='btn btn-primary d-flex justify-content-center'>More info</a>
          </p>
        </div>
      </div>
    <% }) %>
  </div>
</div>

<% include ../partials/footer %>